更新日期：2026-09-07 13:39:48 CST（Asia/Shanghai）

更新说明：用户已确认实施。补充针对实际导出 GLB 的路线碰撞与地面支撑检查脚本，记录几何连通性证据。

# 天窗访谈馆实施方案

## 体验与交付

制作一个可进入、可探索、可阅读和观看访谈的三维账号空间。入口通过错位曲墙遮挡全景；穿过门洞后发现下沉中庭，从宽缓坡道抵达夹层回廊；多个独立侧厅分别承载访谈，沿途展示有来源的观点。中庭帮助理解账号的主题，侧厅提供完整视频观看体验。

视觉依据：[已确认的四视角设计](/Users/lu/Desktop/achoverse/achoverse-marketing/visuals/robotics-spatial/oculus-museum-four-views-v2.png)。保留冷灰白曲墙、深灰石材、圆形天窗与落地光斑。主要复杂度来自高差、错位、门洞和路径。把四张概念图整理成一套连通的建筑平面，处理图中未明确的结构连接与尺度；不机械复制可能不一致的透视。概念图中的人物、文字和几何连线不作为真实访谈资料。

交付独立本地工程、可编辑 Blender 源文件、网页模型、交互与实际浏览器截图。网页实现以四个主要视角的构图、尺度、光照和探索感为美术验收依据，不能用生成图或离线渲染代替运行成果。

## 空间与内容

| 区域 | 建筑组织 | 用户获得的信息与操作 |
|---|---|---|
| 入口叠景 | 厚曲墙、错位门洞、连续天光 | 节目介绍、探索提示，转弯发现中庭 |
| 下沉中庭 | 圆形低台、台阶与可通行连接、上层局部围合 | 主题节点和简明解释，点击查看相关观点与访谈 |
| 对话侧厅 | 独立圆形房间、真实门洞、半圆展陈 | 拨动独立切片，选片前移、邻片让开，打开原片 |
| 夹层回廊 | 宽缓曲坡、连续楼板、护墙、跨层框景 | 发现不同侧厅与观点展位，回望下层但不默认展示全景 |

首版配置至少三个独立访谈展位。目前只有一条真实视频，首个侧厅装入已有访谈与八个精选章节，其他展位清楚显示待加入内容。访谈、章节、观点与展位分别使用 ID；不把一条访谈伪装成多期。节点之间只展示内容支持的关系，编辑概括和来源时间范围明确标注。

精选切片以真实视频帧预览，每片独立、不互相连接；半圆排列，拖动、按钮和键盘都可选择。拖动切片时镜头锁定，结束后恢复。明确播放操作才发声，同一时刻仅一个活动视频。支持完整时间轴、来源跳转、关闭暂停、播放进度与探索位置恢复。

## 实现分工

工程目录：`/Users/lu/Desktop/achoverse/achoverse-marketing/interview-oculus-world/`。采用与前两版相同的 Vite + TypeScript + Three.js，复制适配已验证的内容与交互模块。开发端口计划 4177，启动前检查占用，不占用已有预览端口。

Blender MCP 负责读取当前场景状态、建立独立命名场景、制作建筑、展开 UV 和导出。曲墙、带厚度的圆孔顶板、坡道、楼板、台阶、护墙、门洞和展台都是真实网格。脚本保存为可复现的资产构建流程，源文件单独保存，不覆盖现有 Blender 文件。

Three.js 负责模型加载、材质校准、相机、可读文字、关系装置、切片动画、播放器与输入协调。使用统一布局配置提供楼层高度、门洞位置、路线节点、观察点和展陈锚点；Blender 与网页坐标转换集中处理。

### 光照与材质

先做入口与中庭的实际网页样板，确认厚度、尺度和天光，再补齐侧厅与夹层。使用真实天窗开口投影，校准太阳角度与光斑位置。柔和间接光和接触阴影优先在 Blender 烘焙为静态光照贴图，运行时增加必要的动态阴影。控制实时光与烘焙结果叠加，避免重复曝光。

光斑应随地面、台阶和遮挡体正确变化，不能只在地面贴几个发光圆片。墙面有细微粗糙度与边缘倒角，地砖尺度和接缝统一，避免白色塑料感。Blender 节点与灯光不保证直接导出后效果一致，导入网页后单独校准。首版无镜面水体，无需沿用峡谷的双反射开销。

### 导航与性能

提供沿可通行路线连续前进、分支选择、局部转头和目的地快捷定位。路径覆盖入口、中庭、侧厅、坡道与夹层，镜头保持所在路面的眼高。捷径采用淡出切换到安全观察点，正常探索沿实际路线行进；不把自由飞行当成上下楼。墙体参与点击遮挡，不能隔墙选择展品。

首版采用路线约束导航，不承诺全场任意自由移动的角色物理系统。鼠标、键盘和触摸均提供明确操作。模型按展区组织、复用材质与几何体，静态视频预览与实际播放分离，原片按需加载。提供画质降级，记录实际分辨率、帧率、模型和贴图体积；桌面目标稳定 45–60 FPS，持续低于 30 FPS 必须优化或明确记录限制。

## 实施顺序

1. 明确一套连通布局，制作建筑白模；从四个目标视角检查门洞、坡道、遮挡和上下层关系。
2. 完成入口与中庭材质、光照样板，导入网页检验；据结果完善整馆。
3. 接入全部路线、侧厅切片、观点与来源播放器，完成输入优先级和资源释放。
4. 进行真实浏览器验证、修复、保存截图，记录质量差距，提交独立代码仓库。

建模和坐标布局由同一主线负责。如实施时需要并行，先说明，再把界面适配或独立视觉检查交给子任务，避免同时修改 Blender 场景。

## 逐文件 Todo

以下路径相对本目录；每条仅对应一个文件，新增文件同步补充。

- [x] `implementation-plan.md`：实施范围、顺序和验收标准。
- [x] `README.md`：启动、操作、架构与本地资产路径。
- [x] `validation.md`：实际验证结果、截图说明与限制。
- [x] `.gitignore`：排除依赖、构建、原片、源模型与测试截图。
- [x] `package.json`：依赖、启动、lint、build 和 test 命令。
- [x] `package-lock.json`：锁定安装依赖。
- [x] `tsconfig.json`：严格类型配置。
- [x] `index.html`：网页入口。
- [x] `src/main.ts`：应用装配与生命周期。
- [x] `src/content.ts`：真实访谈、章节、观点与展位数据。
- [x] `src/layout.ts`：布局类型、加载及坐标转换。
- [x] `src/world.ts`：建筑模型、材质与场景装配。
- [x] `src/lighting.ts`：静态光照贴图、实时光及画质配置。
- [x] `src/navigation.ts`：分支路线、上下层移动及安全定位。
- [x] `src/exhibits.ts`：主题装置、墙面观点与来源入口。
- [x] `src/video-slices.ts`：独立半圆切片与拨动动画。
- [x] `src/playback.ts`：单视频、时间定位及恢复状态。
- [x] `src/interface.ts`：导航、阅读与播放器界面。
- [x] `src/styles.css`：灰白视觉和窄屏布局。
- [x] `scripts/build-scene.py`：可复现建筑建模、UV 与导出。
- [x] `scripts/bake-lighting.py`：静态建筑光照烘焙。
- [x] `scripts/denoise-lighting.py`：使用 Blender 合成器降噪烘焙贴图。
- [x] `assets/oculus-world.blend`：独立可编辑模型，本地保存。
- [x] `assets/asset-sources.md`：模型、贴图和媒体来源。
- [x] `public/models/oculus-world.glb`：网页建筑模型。
- [x] `public/models/world-layout.json`：路线、展位与观察点。
- [x] `public/textures/architecture-lightmap.png`：静态光照图集。
- [x] `public/media/interview.mp4`：已有原片本地副本，不提交 Git。
- [x] `public/media/chapter-atlas.jpg`：已有真实章节预览图集。
- [x] `tests/content.test.ts`：章节范围及来源引用检查。
- [x] `tests/navigation.test.ts`：路线连通、楼层高度与分支检查。
- [x] `output/screenshots/entry.png`：真实入口截图。
- [x] `output/screenshots/court.png`：真实下沉中庭截图。
- [x] `output/screenshots/interview.png`：真实切片侧厅截图。
- [x] `output/screenshots/mezzanine.png`：真实夹层截图。
- [x] `output/screenshots/mobile.png`：真实窄屏操作截图。

## 验收与工程约定

四个场景均可到达；入口看不到全景；坡道具有真实高度且不穿顶板；中庭、侧厅与夹层从建筑上连通。切片可逐片选择且不误转镜头；连续时间轴可访问精选章节之外的内容；观点正确定位来源；关闭播放器无声音残留。文字在使用距离可读，待加入展位不显示伪造访谈。

完成类型与未使用代码检查、生产构建、必要测试，并在真实浏览器验证桌面和窄屏输入、加载异常、遮挡点击与性能。没有真实移动设备时，不将窄屏布局验收称为手机 GPU 性能验收。

本轮优先检索 docs 中的 index.md 和代码规范，未发现独立索引或项目级规范文件。已参照 achoverse-code-spec 技能与其引用的前端规范，沿用已确认的原生 Three.js 方案，遵守类型优先、职责分层、单一状态源、资源复用与释放。本工程没有后端改动。

实施前再次检查 Git 边界；按前两版建立独立仓库，完成代码后提交。仅方案阶段不提交。用户已确认后完成代码及建模；交付前完成检查并提交独立仓库。

- [x] `scripts/check-routes.ts`：对实际 GLB 检查路线墙体遮挡、地面支撑与头部净空，输出可重复审计报告。


## 实施完成说明

已完成代码、模型、光照、交互与验证记录。建筑采用60个独立网格；真实光照烘焙包含漫反射颜色，网页不重复叠加同组光照。追加 Blender 合成器降噪与实际 GLB 路线审计。当前夹层观察点设置在高墙末端，自动行走到达后朝向展区。

文件完成不等于概念图逐像素还原。美术、加载体积、真实移动设备和多期内容验证限制见 validation.md。
