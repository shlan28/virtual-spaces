更新日期：2026-09-06 20:23:08 CST（Asia/Shanghai）

更新说明：用户确认后已完成云海空间首版、Blender 地形、视频与观点交互及桌面验证；补充实际新增文件和移动端验证限制。

# 云海访谈空间实施方案

## 目标与参考

在当前项目的 `interview-cloud-world` 中制作可本地运行的 Three.js 空间。参考图为 `/Users/lu/.codex/generated_images/01a0752f-b78c-7542-be89-8aacf2610f39/exec-460be5ac-ca3c-4b45-b140-bc548be9a66b.png`。

保留图中的明亮蓝绿云海、起伏群岛、天然石拱、弯曲石板路、粉色晶体与花木，以及位于不同高度的访谈平台。空间属于访谈账号，机器人访谈是首条真实内容。交付真实三维场景，可改变视角与进入展区；参考图用于审美和构图，不作为整张背景替代三维实现。

首版完整呈现入口、主岛、石拱、低处访谈平台、山顶平台、远景群岛与观点装置。入口采用低处视角，由山体遮挡后续路径；另有全景按钮，方便对照参考图和直接定位。

## 使用体验

1. 进入时看到前景晶体、草坡、向上延伸的步道以及最近的内容入口。
2. 可沿路线前进、拖动观察，也可点击目的地平滑移动。提供回到入口、全景和主题快捷入口，避免必须长距离步行。
3. 访谈在多个局部平台出现。当前真实样本展示一组独立竖直视频切片，沿半圆排列，相邻切片在拨动时让开。保持独立平面，不做相互连接的折纸，不做单个巨大中央屏幕。
4. 鼠标拖动、按钮或键盘可切换片段。浏览不自动有声播放，点击选中片段后从对应时间开始播放；支持暂停、连续定位及关闭返回。使用一个活动视频元素，关闭或切换时停止旧音源。
5. 观点碑、分类装置和小型关系图呈现来自样本的编辑概括，点击可定位来源片段。近处可阅读正文，远处保留标题；详细内容用清晰的网页浮层呈现。
6. 其他访谈平台显示“待加入访谈”，不虚构嘉宾、访谈数量或跨期结论。内容配置支持后续添加多期节目。

## 实现方案

使用独立 Vite + TypeScript + Three.js 工程，避免耦合当前资料目录中的其他项目。采用模块化场景、配置驱动内容与独立相机状态。

Blender MCP 已连接成功，当前场景只有少量对象。实施时在新建独立场景中制作主岛、自然石拱、平台及曲线路径，保存独立 blend 文件，不删除当前已有场景。地形轮廓先渲染检视，再导出 GLB。保留可重复执行的资产生成脚本。

Blender 负责不规则地形与主要美术资产；Three.js 负责浏览、切片动效、视频纹理、可读文字、云雾和水面效果。网页中加载导出 GLB 检查材质，复杂程序纹理转换为兼容材质或烘焙贴图，不假设 Blender 渲染效果可原样导出。

近景采用完整几何，远景使用简化岛屿和天空背景。花草、石头、晶体按可复用资产实例化；云海以分层纹理和雾构建，避免全屏昂贵的体积计算。限制实时阴影、像素比和后处理开销，并提供画质降级。场景以风格化的精细观感为目标，逐步校准轮廓、配色、层次和光照，不承诺生成图的逐像素还原。

优先完成桌面浏览器体验；窄屏提供触摸观察、目的地跳转、切片按钮和适配后的阅读浮层。首版不加入第三人称角色、角色动画、账号后台、多人同步和任意地形自由攀爬。

视频从已有本地 183 MB 文件接入，复制到开发用媒体目录并从 Git 排除。按章节截取封面，不重新转录全片。现有 OCR 时间范围作为初始定位，抽查关键片段；未精校的内容明确标记为编辑整理和约略定位，不伪装逐字引语。

## 项目检查与规则

已优先查找项目 index.md、docs/wiki 索引，未找到既有索引。检索 code spec、code standard、代码规范，仅发现设计文档中的历史检查记录，未找到独立项目规范。

当前父目录不是 Git 仓库。确认后在新目录初始化独立 Git 仓库，完成代码、验证和本地预览后提交 commit。原始视频、node_modules、构建缓存和大体积 blend 源文件不提交；可交付模型与资产生成脚本按体积管理，并在 README 说明。

遵守用户要求：用户已回复“ok”确认本方案，随后开始代码修改。新增代码采用明确类型、场景资源释放、可调参数集中配置、避免全局状态与魔法数的基本约定。

## Todo：每项一个文件

- [x] `implementation-plan.md`：记录范围、分工、文件计划和验收。
- [x] `README.md`：记录启动方式、操作说明、资产来源及已知限制。
- [x] `.gitignore`：忽略依赖、构建结果、本地媒体和大体积源文件。
- [x] `package.json`：定义依赖、开发、构建和检查命令。
- [x] `package-lock.json`：锁定安装依赖。
- [x] `tsconfig.json`：设置 TypeScript 检查。
- [x] `index.html`：页面入口与基础元信息。
- [x] `src/main.ts`：组装应用、加载状态和生命周期。
- [x] `src/styles.css`：界面样式和窄屏适配。
- [x] `src/content.ts`：账号、章节、重点与来源数据。
- [x] `src/world.ts`：装配群岛、灯光、云海、水面和展区。
- [x] `src/vegetation.ts`：生成和布置可复用植被、晶体与石块。
- [x] `src/navigation.ts`：路线浏览、观察、展区跳转与状态恢复。
- [x] `src/video-slices.ts`：半圆布局、拨动动画、选中状态和视频纹理。
- [x] `src/exhibits.ts`：观点碑、分类装置、人物及关系图。
- [x] `src/interface.ts`：阅读层、播放控件、导航与错误提示。
- [x] `scripts/build_scene.py`：可复现的 Blender 地形与资产制作、导出流程。
- [x] `scripts/prepare-media.sh`：准备本地视频与片段缩略图。
- [x] `assets/cloud-world.blend`：保存独立可编辑 Blender 场景。
- [x] `public/models/cloud-world.glb`：导出的网页场景资产。
- [x] `public/media/interview.mp4`：接入用户已有视频，排除 Git。
- [x] `public/media/chapter-atlas.jpg`：真实访谈章节缩略图图集。
- [x] `tests/content.test.ts`：校验片段范围、来源引用和内容 ID 唯一性。
- [x] `validation.md`：记录浏览器操作、性能实测和截图位置。

如制作中需要新增材质贴图或拆分模型，先更新此文档，逐文件记录，不改变已确认的产品范围。

## 验收与交付

- TypeScript 检查与生产构建通过；浏览器无阻断错误、模型和媒体加载失败有明确反馈。
- 在入口、石拱附近、低处平台、山顶平台及全景视角截图，检查构图、亮度、遮挡与景深层次。
- 实际测试进入平台、逐片拨动、选中播放、片段定位、暂停、关闭与返回；确认没有重复音源。
- 测试观点进入来源、镜头位置恢复、窄屏操作、窗口尺寸变化与资源清理。
- 在当前设备记录加载体积和帧率，注明分辨率与画质条件；不把本机结果当成所有设备的保证。
- 最终交付可运行目录、Blender 源文件、GLB、浏览器预览、关键视角截图、操作说明和代码 commit。


## 实施补充

- [x] `src/terrain.ts`：共享地形高度计算与确定性随机数。
- [x] `public/models/world-layout.json`：Blender 导出的场景布局参数。
- [x] `public/textures/cloud-panorama.png`：为远景生成的云海天空纹理。
- [x] `assets/sky-prompt.txt`：记录云海纹理的生成方式与完整提示词。

桌面交互、模型导入、播放定位、观点来源跳转和构建已验证。移动视口覆盖在当前浏览器未生效，因此真实窄屏与手机 GPU 验证未完成；详见 validation.md。当前为风格化可运行版本，不声称已还原概念图的全部美术细节。
