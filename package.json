{
  "name": "interview-oculus-world",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4177 --strictPort",
    "build": "tsc --noEmit && vite build",
    "lint": "tsc --noEmit --noUnusedLocals --noUnusedParameters",
    "test": "tsx --test tests/*.test.ts",
    "check:routes": "tsx scripts/check-routes.ts"
  },
  "dependencies": {
    "three": "0.180.0"
  },
  "devDependencies": {
    "@types/three": "^0.180.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.5",
    "tsx": "^4.20.5",
    "@types/node": "^22.18.0"
  }
}
